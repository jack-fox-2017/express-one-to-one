<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>edit prodiles</title>
  </head>
  <body>
    <h1>Edit Profiles</h1>
    <form class="" action="/profiles/edit/<%=  data[0].id %>" method="post">
      <label>Id Account</label>
      <input type="text" name="id_name" value="<%= data[0].id_name %>">
      <label>User Name</label>
      <input type="text" name="username" value="<%= data[0].username %>">
      <label>Password</label>
      <input type="text" name="password" value="<%= data[0].password %>">
      <label>Contacts Id</label>
      <input type="text" name="contact_id" value="<%= data[0].contact_id %>">
      <button type="submit" name="button">Save</button>
    </form>
  </body>
</html>
